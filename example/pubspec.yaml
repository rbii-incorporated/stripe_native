name: stripe_native_example
description: Demonstrates how to use the stripe_native plugin.
publish_to: 'none'

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: any

  stripe_native:
    path: ../

flutter:

  uses-material-design: true

